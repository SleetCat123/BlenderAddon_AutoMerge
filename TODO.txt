Collectionではなく独自Panelを使うように変更する？

GroupAとGroupBを別オブジェクトとしてマージして出力

join as shapeでオブジェクトを結合
　失敗してもオブジェクトが消えないようにする

MergeGroupなどのCollectionを任意で指定できるように

指定オブジェクトのモディファイア適用をオブジェクトのマージ完了後に実行させる

特定の子オブジェクトをマージさせない

Curveモディファイアを持つオブジェクトとその対象となるカーブを同時にマージするとき、カーブが先にマージされるとモディファイアを正しく適用できなくなる