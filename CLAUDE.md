
- Blenderアドオン
- 常に日本語で回答
- 常に日本語で回答
# これを読んだら以下の内容を**必ず**全文復唱してください。
- 単一責任の原則を意識して実装。必要に応じてファイル分割とフォルダ分けを行う
- 後方互換性の考慮禁止
- コメントは日本語で記載
- きりのいいタイミングでgit commit

# 🚨 重要：Git操作の注意点
- **このプロジェクトはサブモジュールです**
- **git commitは必ずこのサブモジュール内でのみ実行**
- **メインリポジトリ（親ディレクトリ）では絶対にcommitしない**
- サブモジュールの更新をメインリポジトリに反映する作業は行わない

## コーディング規約

### ファイル構造
- `__init__.py`: アドオンのエントリーポイント
- `scripts/`: 機能ごとに分離されたスクリプト
  - `funcs/`: 各機能の実装
  - `ops/`: Blenderオペレーター
  - `menu/`: メニュー関連
  - `link/`: 他アドオンとの連携

### 命名規則
- オペレーターID: `object.shapekeys_util_*`
- 関数名: `func_*` プレフィックスを使用
- 定数: 大文字スネークケース（例: `FORCE_APPLY_MODIFIER_PREFIX`）

## リリース時の注意
1. バージョン番号を`__init__.py`で更新
2. CHANGELOG.mdに変更内容を記載
3. README.mdを最新の機能に合わせて更新
4. 古いバージョンからの移行注意事項を確認

## Gitコミットメッセージルール

### フォーマット
```
[prefix]: 変更内容の要約

詳細な説明（必要に応じて）
```

### プレフィックス
- `feat`: 新機能追加
- `fix`: バグ修正
- `docs`: ドキュメント変更
- `style`: コードスタイルの変更（機能に影響しない）
- `refactor`: リファクタリング
- `test`: テスト追加・修正
- `chore`: ビルドプロセスやツールの変更

### 例
```
feat: Apply Selected Modifiers機能を追加

選択中のモディファイアのみを適用する機能を実装。
%AS%などの特殊接頭辞もサポート。
```

### 注意事項
- 日本語でのコミットメッセージ可
- 変更内容を簡潔かつ明確に記述
- 破壊的変更がある場合は明記